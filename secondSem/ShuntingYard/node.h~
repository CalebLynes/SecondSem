//node.h
#ifndef NODE_H
#define NODE_H

#include <iostream>
using namespace std;

class Node {
    public:
        Node(char c, Node* n);
        ~Node();
        char data;
        Node* left;
        Node* right;
};

#endif
